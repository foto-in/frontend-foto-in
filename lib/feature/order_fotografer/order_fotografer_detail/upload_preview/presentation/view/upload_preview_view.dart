import 'package:flutter/cupertino.dart';

import '../widget/upload_preview_widget.dart';

class UploadPreviewView extends StatelessWidget {
  static const String routeName = '/upload-preview';
  const UploadPreviewView({super.key});

  @override
  Widget build(BuildContext context) {
    return const UploadPreviewWidget();
  }
}
